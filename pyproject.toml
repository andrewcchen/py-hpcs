[project]
name = "py-hpcs"
version = "0.1.0"
description = "Utilities for capturing spectra from HPCS-310 spectrometers"
requires-python = ">=3.12"
dependencies = [
    "bleak==2.*",
]

[tool.uv]
package = false
